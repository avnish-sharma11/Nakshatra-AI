export type KundliInput = {
  birthdate: string;    // format: dd-mm-yyyy
  birthtime: string;    // format: HH:MM
  birthlatitude: string;
  birthlongitude: string;
  birthtimezone: string; // e.g. "5.5"
};
